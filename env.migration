# MongoDB Configuration for Migration
MONGODB_URI=mongodb://localhost:27017/smart-leader-real-estate

# Firebase Configuration (already configured in frontend/lib/firebase.js)
# No additional configuration needed for Firebase

